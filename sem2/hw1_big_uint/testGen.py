from random import randrange
command = input()

# Just generating BigUint number with 100-1000 digits count:
print(randrange(10 ** randrange(100, 1000)))

# Printing command, that was received from the bash
print(command)

# Just generating BigUint number with 100-1000 digits count:
print(randrange(10 ** randrange(100, 1000)))
